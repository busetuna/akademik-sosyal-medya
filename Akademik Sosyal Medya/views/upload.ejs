<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>PDF Yükleme</title>
  <style>
    body {
      font-family: sans-serif;
      background: #f8f9fa;
      padding: 40px;
    }

    .upload-form {
      background: white;
      padding: 30px;
      border-radius: 12px;
      max-width: 600px;
      margin: auto;
      box-shadow: 0 4px 16px rgba(0,0,0,0.1);
    }

    .upload-form input[type="file"] {
      display: block;
      margin-bottom: 20px;
    }

    .upload-form button {
      background: #4c6ef5;
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-weight: bold;
    }

    .result {
      background: #e6f4ea;
      padding: 20px;
      margin-top: 30px;
      border-radius: 8px;
      white-space: pre-line;
    }
  </style>
</head>
<body>
  <div class="upload-form">
    <h2>📄 Birden Fazla PDF Yükle</h2>
   <form action="/upload" method="POST" enctype="multipart/form-data">
  <input type="file" name="pdfs" accept="application/pdf" multiple required />
  <br><br>
  <button type="submit">Yükle ve Özet Çıkar</button>

</form>


    <% if (abstracts && abstracts.length) { %>
      <div class="result">
        <h3>📚 Çıkarılan Özetler</h3>
        <% abstracts.forEach((abs, i) => { %>
          <p><strong>Dosya <%= i + 1 %>:</strong><br><%= abs %></p>
        <% }) %>
      </div>
    <% } %>
  </div>
</body>
</html>
